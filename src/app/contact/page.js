import ContactForm from "../components/ContactForm";

export const metadata = {
  title: "Contact",
  description:
    "Contact Kaweesi Matia about full-stack software engineering roles. Typically replies within one business day.",
};

export default function ContactPage() {
  return <ContactForm />;
}
